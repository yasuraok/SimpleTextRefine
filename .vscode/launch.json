{
    "version": "0.0.1",
    "configurations": [
        {
            "name": "Extension",
            "type": "extensionHost",
            "request": "launch",
            "runtimeExecutable": "${execPath}",
            "args": [
                "--workspaceFolder=${workspaceFolder}",
                "--extensionDevelopmentPath=${workspaceFolder}"
            ]
        }
    ]
}